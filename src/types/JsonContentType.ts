type JsonContentType = {
  url_cover: string;
  url_video: string;
  url_linkedin: string;
  url_live_project: string;
  technologies: string[]; // technologies é um array de strings
};

export default JsonContentType;
